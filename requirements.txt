# Host-side tooling deps for spinesurg-ct-nnunet.
# nnU-Net v2 and its heavy deps (torch, MONAI, etc.) live inside the
# Singularity container -- see containers/spinesurg-ct.def.

numpy>=1.24
nibabel>=5.0
scipy>=1.10          # distance transforms for the derived disc-space class
matplotlib>=3.7
scikit-learn>=1.3
pandas>=2.0
huggingface_hub>=0.20

# Dev extras (optional)
# pytest>=7.4
# ruff>=0.4
